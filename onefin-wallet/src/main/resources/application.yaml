spring:
  application:
    name: OneFin Wallet
  datasource:
    url: jdbc:mysql://localhost:3306/onefin_db?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
